# Source this file to set up the Java classpath environment variable so that the java interpretter
# knows where to find the spoke length calculator's classes.

export CLASSPATH="${CLASSPATH}:./target/classes"

# vim: ts=4 sw=4 et
